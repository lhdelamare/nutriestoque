{
  "name": "nutri-estoque-escolar",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Controle de Estoque de Compras e Alimentos Escolar com FEFO e Etiquetas de Validade Fracionada",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"red,cyan\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "setup": "npm install && cd server && npm install && npx prisma db push && npm run db:seed && cd ../client && npm install",
    "build": "cd server && npm run build && cd ../client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
